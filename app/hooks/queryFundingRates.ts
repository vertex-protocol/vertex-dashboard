// TODO: finish calculating funding rates
export function queryFundingRates(data: any, type: string) {
  const result: number[] = [];

  if (type == 'hourly') {
  } else if (type == 'daily') {
  } else if (type == 'annual') {
  }

  return result;
}
